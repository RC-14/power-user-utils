{
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc",
    "format": "prettier . --write",
    "build": "node scripts/build.js",
    "start": "web-ext --config=web-ext-config.mjs run",
    "package": "web-ext --config=web-ext-config.mjs build",
    "clean": "rm -rf build dist"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@total-typescript/ts-reset": "^0.6.1",
    "@types/webextension-polyfill": "^0.12.3",
    "esbuild": "0.25.5",
    "prettier": "3.6.0",
    "typescript": "^5.8.3",
    "web-ext": "^8.8.0"
  }
}
